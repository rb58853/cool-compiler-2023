class Main{
    main(): Int { 
        let x:Int <- 2, y:Int<- x + test(test(test(10,10),x),2) in{
            y;
        }
        
    };

    test(a:Int, b:Int):Int {
        {
            let x:Int<- 10 in {
               a <- b + a + x;
            };
            -- a;
        }
    };    
};
