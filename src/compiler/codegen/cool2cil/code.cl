class Main{
    main(): Int { 
    {
        let x:Int <- 2 + let x:Int<-1 in x+ let y:Int <- x+2 in y + x, b:Int<-0 in {
        -- let x:Int <- 20 in {
            x <- 3 + x;
            while 5 < x loop
            x <- x-1
            pool;
            x;
        };
        1;
    }
        
    };    
};